<h1>Tasks</h1>

<%= link_to 'New task', new_task_path %>
<hr>

<% @tasks.each do |task|%>
    <p><%= link_to task.name, task_url(task) %></p>
    <p><%=    %></p>
    <small><%=  "#{task.created_at}   #{task.name}" %></small>
    <hr>
<% end %>